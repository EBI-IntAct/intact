<?xml version="1.0" encoding="ISO-8859-1" ?>

<!DOCTYPE struts-config PUBLIC
          "-//Apache Software Foundation//DTD Struts Configuration 1.0//EN"
          "http://jakarta.apache.org/struts/dtds/struts-config_1_1.dtd">

<!--
This is the Struts configuration file for the example application,
using the proposed new syntax.

NOTE:  You would only flesh out the details in the "form-bean"
declarations if you had a generator tool that used them to create
the corresponding Java classes for you.  Otherwise, you would
need only the "form-bean" element itself, with the corresponding
"name" and "type" attributes.
-->

<struts-config>

    <!-- ========== Form Bean Definitions =================================== -->
    <form-beans>

        <!-- The filter form -->
        <form-bean name="filterForm"
                   type="uk.ac.ebi.intact.application.statisticView.struts.view.FilterForm"/>

    </form-beans>

    <!-- ========== Global Forward Definitions ============================== -->
    <global-forwards>

        <forward name="start"   path="/do/init" />

    </global-forwards>

    <!-- ========== Action Mapping Definitions ============================== -->
    <action-mappings>

        <!-- Action mapping for the Statistic application -->
        <!-- Entrance of the application -->
        <action path="/init"
                type="org.apache.struts.actions.ForwardAction"
                parameter="statisticView.layout">
        </action>

        <!-- data filtering -->
        <action path="/filter"
                type="uk.ac.ebi.intact.application.statisticView.struts.controller.FilterAction"
                name="filterForm"
                validate="false"
                scope="request">

            <forward name="success" path="statisticView.layout"/>

        </action>

    </action-mappings>

    <!-- ========== Plug Ins Configuration ================================== -->

    <plug-in className="org.apache.struts.tiles.TilesPlugin" >
        <set-property property="definitions-config" value="/WEB-INF/tiles-defs.xml"/>
        <set-property property="moduleAware" value="true" />
    </plug-in>

    <!-- ========== Message Resources Definitions =========================== -->

    <message-resources
        parameter="uk.ac.ebi.intact.application.statisticView.MessageResources"/>

</struts-config>