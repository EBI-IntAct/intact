<project>
    <build>
        <plugins>
            <plugin>
                <artifactId>psimitab-maven-plugin</artifactId>

                <configuration>
					<!-- These parameters are going to be set at runtime -->
                    <binaryInteractionClass>lala2</binaryInteractionClass>
                    <columnHandler>foobar2</columnHandler>

                    <tabFile></tabFile>

                    <files>
                        <file></file>
                    </files>

                </configuration>
            </plugin>
        </plugins>
     </build>
</project>