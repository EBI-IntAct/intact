<project>
    <build>
        <plugins>
            <plugin>
                <artifactId>psimitab-maven-plugin</artifactId>

                <configuration>
					<!-- These parameters are going to be set at runtime -->
                    <binaryInteractionClass>lala</binaryInteractionClass>
                    <columnHandler>foobar</columnHandler>

                    <directoryPath></directoryPath>

                </configuration>
            </plugin>
        </plugins>
     </build>
</project>