<project>

    <build>
        <plugins>
            <plugin>
                <artifactId>update-db-proteins-maven-plugin</artifactId>
                <configuration>
                    <!-- Leave configuration to the test -->
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>