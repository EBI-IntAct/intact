<!-- This file contains the hibernate configuration, needed when accessing locally
to an IntAct instance -->
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- ////////////////////////////////////////////////////////////////////// -->
        <!-- // ESSENTIAL CONFIGURATION                                          // -->
        <!-- ////////////////////////////////////////////////////////////////////// -->

        <!-- Database configuration information -->
        <!-- In this example a temporary H2 database is used, so we need to configure
        its dialect, driver, username, password and URL -->
        <property name="dialect">org.hibernate.dialect.H2Dialect</property>
        <property name="connection.driver_class">org.h2.Driver</property>
        <property name="connection.username">sa</property>
        <property name="connection.password"></property>
        <property name="connection.url">jdbc:h2:target/intact-db</property>

        <!-- This property defines the behaviour of the database when hibernate is initialized.
             It can be "create", "create-drop" or "update", generally. The first two will create
             an empty instance of the database (removing any existing data!), "update" will create
             the database if necessary, not removing existing data -->
        <property name="hbm2ddl.auto">update</property>


        <!-- ////////////////////////////////////////////////////////////////////// -->
        <!-- // ADVANCED CONFIGURATION                                           // -->
        <!-- ////////////////////////////////////////////////////////////////////// -->

        <!-- Connection provider -->
        <property name ="connection.provider_class">org.hibernate.connection.C3P0ConnectionProvider</property>

        <!-- Use the C3P0 connection pool. -->
        <!--<property name="c3p0.min_size">3</property>-->
        <!--<property name="c3p0.max_size">5</property>-->
        <!--<property name="c3p0.timeout">1800</property>-->

        <!-- Disable second-level cache. -->
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>
        <property name="cache.use_query_cache">false</property>
        <property name="cache.use_minimal_puts">false</property>
        <property name="max_fetch_depth">3</property>

        <!-- Print SQL to stdout. -->
        <property name="show_sql">false</property>
        <property name="format_sql">false</property>

        <!-- Bind the getCurrentSession() method to the thread. -->
        <property name="current_session_context_class">thread</property>


    </session-factory>

</hibernate-configuration>
