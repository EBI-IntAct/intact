<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context-2.5.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx-2.5.xsd">

    <!-- This file contains the dabatase configuration to start IntAct in these examples -->

    <import resource="base.spring.xml"/>

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="properties">
            <props>
                <!-- JDBC connection URL -->
                <prop key="db.jdbcurl">jdbc:h2:file:intact-kickstart-db;LOCK_MODE=0</prop>

                <!-- JDBC Driver class name -->
                <prop key="db.driver">org.h2.Driver</prop>

                <!-- Hibernate Dialect -->
                <prop key="db.dialect">org.hibernate.dialect.H2Dialect</prop>

                <!-- Database user name -->
                <prop key="db.user">sa</prop>

                <!-- Database user password -->
                <prop key="db.password"></prop>

                <!-- DDL behaviour. Use "create" to create the database schema automatically when the IntactContext
                is started. Use "none" to use an existing database.-->
                <prop key="db.hbm2ddl">create</prop>
            </props>
        </property>
    </bean>

</beans>
