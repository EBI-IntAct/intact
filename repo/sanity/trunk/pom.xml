<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <parent>
        <groupId>uk.ac.ebi.intact.maven</groupId>
        <artifactId>intact-hibernate-master</artifactId>
        <version>17</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>

    <groupId>uk.ac.ebi.intact.sanity</groupId>
    <artifactId>sanity-master</artifactId>
    <packaging>pom</packaging>
    <version>2.0-SNAPSHOT</version>

    <name>IntAct Sanity</name>
    <description>Sanity Master POM</description>
    <url>http://www.ebi.ac.uk/~intact/devsite/sanity</url>

    <properties>
        <currentVersion>${version}</currentVersion>
        <core.version>1.6.2-SNAPSHOT</core.version>
    </properties>

    <scm>
        <connection>scm:svn:https://intact.svn.sourceforge.net/svnroot/intact/repo/sanity/trunk</connection>
        <developerConnection>scm:svn:https://intact.svn.sourceforge.net/svnroot/intact/repo/sanity/trunk
        </developerConnection>
        <url>http://svn.sourceforge.net/viewcvs.cgi/intact/repo/sanity/trunk</url>
    </scm>

    <modules>
        <module>intact-sanity-check</module>
        <module>sanity-maven-plugin</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- this group -->
            <dependency>
                <groupId>uk.ac.ebi.intact.sanity</groupId>
                <artifactId>intact-sanity-check</artifactId>
                <version>${currentVersion}</version>
            </dependency>

            <!-- other intact -->
            <dependency>
                <groupId>uk.ac.ebi.intact.commons</groupId>
                <artifactId>intact-commons</artifactId>
                <version>2.1.2-SNAPSHOT</version>
            </dependency>
            <dependency>
                <groupId>uk.ac.ebi.intact</groupId>
                <artifactId>intact-core</artifactId>
                <version>${core.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.ac.ebi.intact.core</groupId>
                <artifactId>intact-persister</artifactId>
                <version>${core.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.ac.ebi.intact.datasets</groupId>
                <artifactId>intact-dataset</artifactId>
                <version>1.6.0</version>
                <scope>test</scope>
            </dependency>

            <!-- third party -->
            <dependency>
                <groupId>log4j</groupId>
                <artifactId>log4j</artifactId>
                <version>1.2.13</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.3.1</version>
        </dependency>
    </dependencies>

    <!-- EBI repositories -->
    <repositories>
        <repository>
            <id>ebi-repo</id>
            <name>The EBI internal repository</name>
            <url>http://www.ebi.ac.uk/~maven/m2repo</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>ebi-repo-snapshots</id>
            <name>The EBI internal repository</name>
            <url>http://www.ebi.ac.uk/~maven/m2repo_snapshots</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <distributionManagement>
        <site>
            <id>intact-site</id>
            <name>Intact site</name>
            <url>${site.remote.basedir}/sanity</url>
        </site>
    </distributionManagement>

</project>