<project>
    <build>
        <plugins>
            <plugin>
                <artifactId>psimitab-maven-plugin</artifactId>

                <configuration>
					<!-- These parameters are going to be set at runtime -->
                    <binaryInteractionClass>uk.ac.ebi.intact.psimitab.IntActBinaryInteraction</binaryInteractionClass>
                    <columnHandler>uk.ac.ebi.intact.psimitab.IntActColumnHandler</columnHandler>

                </configuration>
            </plugin>
        </plugins>
     </build>
</project>